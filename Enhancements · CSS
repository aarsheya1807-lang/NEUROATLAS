/* enhancements.css
   Header credit + toolbar + shared panel for the 4 new tools.
   Uses the SAME variables already defined in style.css. */

/* ---- Header credit (right side) ---- */
.header-credit {
  margin-left: auto;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  font-family: var(--mono);
  line-height: 1.3;
}
.header-credit .credit-name {
  font-size: 12.5px;
  font-weight: 600;
  color: var(--text);
  letter-spacing: 0.02em;
}
.header-credit .credit-tagline {
  font-size: 10.5px;
  color: var(--muted);
  letter-spacing: 0.01em;
}
@media (max-width: 860px) {
  .header-credit { display: none; } /* avoid crowding the mobile header/toggle */
}

/* ---- Toolbar (bottom-left, mirrors your existing viewport-controls style) ---- */
.enh-toolbar {
  position: absolute;
  bottom: 18px;
  left: 18px;
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  z-index: 12;
  max-width: 60%;
}
.enh-tool-btn {
  background: var(--surface);
  border: 1px solid var(--line);
  color: var(--text);
  font-family: var(--mono);
  font-size: 11.5px;
  padding: 9px 13px;
  border-radius: 8px;
  transition: border-color 0.15s;
}
.enh-tool-btn:hover { border-color: var(--synapse); }
.enh-tool-btn.on { color: var(--clinical); border-color: var(--clinical); }

@media (max-width: 860px) {
  .enh-toolbar { left: 12px; right: 12px; max-width: none; bottom: auto; top: 64px; }
}

/* ---- Shared panel (vascular / lesion / network / vignette) ---- */
.enh-panel {
  position: absolute;
  top: 74px;
  left: 18px;
  width: 320px;
  max-height: calc(100% - 130px);
  overflow-y: auto;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 12px;
  padding: 18px;
  z-index: 16;
  transform: translateX(0);
  opacity: 1;
  transition: transform 0.25s ease, opacity 0.25s ease;
}
.enh-panel.enh-hidden {
  transform: translateX(-120%);
  opacity: 0;
  pointer-events: none;
}

.enh-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 10px;
}
.enh-title {
  font-family: var(--mono);
  font-size: 13px;
  font-weight: 600;
  letter-spacing: 0.03em;
  color: var(--text);
}
.enh-close {
  background: none;
  border: none;
  color: var(--muted);
  font-size: 17px;
  cursor: pointer;
  padding: 2px 8px;
  border-radius: 6px;
}
.enh-close:hover { background: var(--surface-2); color: var(--text); }

.enh-hint {
  font-size: 12.5px;
  color: var(--muted);
  margin: 0 0 12px;
  line-height: 1.5;
}
.enh-empty { color: var(--muted); font-size: 13px; }

.enh-list { display: flex; flex-direction: column; gap: 6px; margin-bottom: 12px; }
.enh-list-item {
  background: var(--surface-2);
  border: 1px solid var(--line);
  color: var(--text);
  text-align: left;
  font-family: var(--sans);
  font-size: 13px;
  padding: 9px 12px;
  border-radius: 6px;
}
.enh-list-item:hover { border-color: var(--synapse); }

.enh-sublabel {
  font-family: var(--mono);
  font-size: 10.5px;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--muted);
  margin: 12px 0 4px;
}

.enh-syndrome {
  background: var(--surface-2);
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 10px 12px;
  margin-bottom: 8px;
}
.enh-syndrome strong { font-size: 12.5px; color: var(--text); }
.enh-syndrome p { font-size: 12.5px; color: var(--text); margin: 4px 0 0; line-height: 1.5; }

.enh-caveat {
  margin-top: 12px;
  font-size: 11.5px;
  color: var(--clinical);
  line-height: 1.5;
}

.enh-vignette-text {
  font-size: 13.5px;
  line-height: 1.6;
  color: var(--text);
  background: var(--surface-2);
  border-left: 2px solid var(--synapse);
  padding: 10px 12px;
  border-radius: 0 8px 8px 0;
  margin: 8px 0 12px;
}
.enh-score {
  font-family: var(--mono);
  font-size: 11.5px;
  color: var(--synapse);
  margin: 0 0 8px;
}
.enh-feedback { font-family: var(--mono); font-size: 13px; font-weight: 600; margin: 10px 0 4px; }
.enh-feedback.enh-correct { color: var(--synapse); }
.enh-feedback.enh-wrong { color: var(--danger); }
.enh-next-btn {
  margin-top: 12px;
  background: var(--surface-2);
  border: 1px solid var(--synapse);
  color: var(--synapse);
  font-family: var(--mono);
  font-size: 12px;
  padding: 8px 14px;
  border-radius: 8px;
}
.enh-next-btn:hover { background: rgba(71, 224, 176, 0.1); }

@media (max-width: 860px) {
  .enh-panel { left: 12px; right: 12px; width: auto; top: auto; bottom: 18px; max-height: 50%; }
}
